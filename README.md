# sanchez-pair-programming
For short assignment 05. 

My function takes in an r value and a theta value as inputs to describe a complex number in polar form: re^(itheta). Then it outputs the Cartesian coordinates (x and y) for that value. It is expected that r is a positive, real number, and that theta is a positive real number corresponding to an angle in radians. 
